import { html } from 'lit-html'

export const Header = () => html` <header class="header">HEADER</header> `

export default {
  title: 'Components/Header',
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
}
